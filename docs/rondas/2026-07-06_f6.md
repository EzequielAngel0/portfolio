# Ronda 2026-07-06 · Fase F6 (pulido y QA, gate de merge)

Cierra el gate de calidad del doc 06 antes del merge a `master` (D3 del dueño): pase de polish `make-interfaces-feel-better`, auditorías `web-design-guidelines` y `review-animations`, Lighthouse y presupuesto, verificación end-to-end automatizada, validación estructural de JSON-LD/OG, reglas duras y code review del diff completo. Rama de trabajo: `feat/pulido-qa` desde `develop`. Las skills de auditoría se cargaron desde `.agents/skills/` (no estaban instaladas como skills de sesión).

## Orden de trabajo y estado

- [hecho] Pase de polish `make-interfaces-feel-better` (tablas antes/después abajo).
- [hecho] Auditoría `web-design-guidelines` (guías frescas de Vercel) con correcciones.
- [hecho] Auditoría `review-animations` con corrección (skip link) y veredicto Approve.
- [hecho] Presupuesto de peso sobre `dist/` y Lighthouse móvil/desktop (4 corridas).
- [hecho] Verificación e2e con Playwright contra `astro preview` (34 checks).
- [hecho] Validación estructural de JSON-LD/OG/hreflang/sitemap/robots (56 checks).
- [hecho] Pase de reglas duras por grep sobre `src/`.
- [hecho] Contraste AA verificado por cálculo WCAG sobre los pares de tokens.
- [hecho] `/code-review` multi-agente del diff `master..develop` + `frontend-code-review`.
- [pendiente del dueño] Pase manual NVDA (guion abajo) y validadores externos (guion abajo).
- [bloqueado] Beacon de Cloudflare + CSP: sigue esperando D7.

## Pase de polish (make-interfaces-feel-better)

Formato antes/después que exige la skill, agrupado por principio. Todo verificado con `check` + `build` + e2e.

### Sombras en capas en vez de bordes (principio 3 + doc 05 "profundidad por sombras suaves")

| Antes | Después |
| --- | --- |
| Tarjetas de `Projects.astro` y `Certifications.astro` con `border border-line` | `shadow-card`: token `--card-shadow` (anillo 1px + 2 capas suaves en claro; solo anillo blanco puro en oscuro) |
| Corte de caja de `CaseStudyArticle.astro` con `border border-line` | `shadow-card` |
| `.diagram-scroll` (diagrama) con `border: 1px solid var(--color-line)` | `box-shadow: var(--card-shadow)` |
| `.carousel-slide` con `border: 1px solid var(--color-line)` | `box-shadow: var(--card-shadow)` |

Los separadores (border-t de secciones, border-b del header, borde del TOC) se quedan como bordes: la skill lo pide explícito (dividers no son profundidad).

### Radios concéntricos (principio 1)

| Antes | Después |
| --- | --- |
| `.carousel-slide` radio 6px + padding 8px + imagen 4px (6 != 4+8) | radio 10px + padding 6px + imagen 4px (10 = 4 + 6, exacto) |

Revisados sin cambio: tarjetas con `p-6` (24px) superan el umbral de la skill para exigir concentricidad (capas independientes); botones y toggles no anidan superficies redondeadas.

### Outline de imagen (principio 11 + doc 05)

| Antes | Después |
| --- | --- |
| `.slide-image` sin outline | `outline: 1px solid rgb(0 0 0 / 0.1)` inset; en oscuro `rgb(255 255 255 / 0.1)` (negro/blanco puros, nunca tintados) |

### Tipografía (principio 10 + guía Vercel)

| Antes | Después |
| --- | --- |
| `text-wrap: pretty` solo en `p` | `p, li, figcaption, blockquote` |
| Apóstrofe recto en `contact.lead` EN ("you're") | Apóstrofe tipográfico ("you’re") |

Revisados sin cambio: `balance` en h1-h3 ya estaba; `tabular-nums` global sobre mono ya estaba; font smoothing ya estaba.

### Estados hover y press (principios 4 y 12 + guía Vercel "todo interactivo necesita hover")

| Antes | Después |
| --- | --- |
| Toggles de tema/idioma sin estado hover | `hover:border-ink/35` + `transition-[transform,border-color]` |
| Botones del carrusel sin hover | Igual que los toggles |
| Logo del header sin hover | `hover:underline` (mismo lenguaje que `aria-current`) |
| Press feedback con easing default de Tailwind | `ease-out` explícito en Button, toggles, carrusel e iconos de links |

Revisados sin cambio: `active:scale-[0.96]` ya estaba en el rango correcto (0.95-0.98); cero `transition: all`; cero `will-change`.

### Comunicar estado, no solo pintarlo (doc 06 toggles)

| Antes | Después |
| --- | --- |
| Toggle de tema siempre `[ ]` (estado solo en aria-label) | `[ ]` en claro, `[*]` en oscuro (variante `dark:` por CSS, ancho estable en mono) |

### Accesibilidad y plataforma (guía Vercel)

| Antes | Después |
| --- | --- |
| Links externos sin aviso de pestaña nueva | `<span class="sr-only">(se abre en una pestaña nueva)</span>` localizado (`a11y.newTab`) en credenciales, repo y redes |
| Sin `meta theme-color` | Meta sincronizada con el tema (`#f6f5f1` / `#121417`, espeja `--paper`) desde el anti-FOUC y el toggle |
| Sin `touch-action` | `touch-action: manipulation` en `a, button` + `-webkit-tap-highlight-color: transparent` |
| Track del carrusel enfocable sin nombre accesible | `aria-label` con el título de la sección |
| Toggle de idioma sin `lang` | `lang={otherLocale}` (NVDA pronuncia el label en su idioma) |

### Motion en acciones de teclado (review-animations, regla 2)

| Antes | Después |
| --- | --- |
| Skip link con `transition: top 0.15s ease` (acción de teclado animando propiedad de layout) | Sin transición: aparece al instante |

## Auditoría review-animations (veredicto)

Inventario completo del motion contra STANDARDS: boot del hero (0.5s power2.out, stagger 80ms dentro del rango 30-80ms, corre una vez), revelado por sección (`once: true`, solo autoAlpha+y), pulso del punto (solo opacity, único loop, apagado por reduced motion), snap del carrusel (interrumpible: `killTweensOf` en press, tween solo de `scrollLeft`), estados interactivos por CSS transitions (interrumpibles), `hover:` gateado por `(hover: hover)` (default de Tailwind 4), `prefers-reduced-motion` con estado final instantáneo (global.css + `gsap.matchMedia` + carrusel sin Draggable). Hallazgo corregido: la transición del skip link (tabla arriba). **Veredicto: Approve.**

## Lighthouse y presupuesto (doc 06)

Sobre `astro preview` local (Lighthouse 13.4, Chrome headless). Objetivos: Perf >= 95, A11y = 100, SEO = 100, BP = 100.

| Corrida | Perf | A11y | BP | SEO | LCP | CLS | TBT |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Home móvil | 100 | 100 | 100 | 100 | 1.7 s | 0.011 | 0 ms |
| Case study móvil | 99 | 100 | 100 | 100 | 1.8 s | 0.006 | 0 ms |
| Home desktop | 100 | 100 | 100 | 100 | 0.4 s | 0.035 | 0 ms |
| Case study desktop | 100 | 100 | 100 | 100 | 0.4 s | 0 | 0 ms |

Presupuesto sobre `dist/` (gzip), tras el gate del code review:

- **JS en páginas animadas: 44.7 KB** (gate de Base 0.3 + preload-helper 0.7 + motion 17.2 + GSAP core 26.5), presupuesto < 60 KB. **404 y usuarios con reduced motion: ~1 KB** (solo el gate; antes descargaban los ~44 KB completos). Draggable (12.8 KB) y el script del carrusel (0.9 KB) quedan en chunks propios que ninguna página referencia mientras no haya capturas (D8), y con capturas solo viajan sin reduced motion.
- **CSS: 5.3 KB** (< 50 KB). **Fuentes: 3 woff2, 63 KB** (<= 3 archivos). HTML 2.4-7.8 KB por página.
- LCP < 1.8 s, CLS ~0, TBT < 150 ms: cumplidos (tabla). Lighthouse re-corrido tras los fixes: home móvil 99-100 (varianza local), resto igual.
- Trade-off aceptado del gate: el boot del hero arranca un instante después del primer pintado (un roundtrip extra por el import dinámico); el contenido nace visible, así que no hay flash de contenido oculto.

## Verificación e2e (Playwright sobre preview, 34 checks en verde)

`playwright-core` vía `npm i --no-save` (lockfile intacta, verificado con git) usando el Chrome local. Flujo completo del doc 07:

- Detección de idioma: navegador EN redirige `/` a `/en/` una vez, guarda `lang-pref`, sin ping-pong al volver a `/`; navegador ES se queda en `/`.
- Tema: claro por defecto, click cambia a oscuro (data-theme + localStorage + meta theme-color + aria-label + glifo `[*]`), persiste tras recargar, operable con Enter.
- Case study: CTA del hero navega, h1 correcto, las 8 anclas del TOC existen como ids reales, diagrama con `role="img"` + title, toggle de idioma apunta al equivalente exacto (`/en/acp-suite/`).
- Credenciales: 3 links externos con `noopener noreferrer` y aviso sr-only. CV: atributo `download` y PDF 200.
- 404: ruta inexistente responde HTTP 404 con bloques ES (h1) / EN (h2) y links de salida.
- Sin JS: `/`, `/acp-suite/` y `/en/` con 0 nodos ocultos (mejora progresiva verificada).
- Teclado: primer foco el skip link (visible al enfocar, anillo de 2px), header completo tabulable, toggle con Enter.
- Reduced motion: pulso a duración ~0 x1 y hero visible sin tweens.
- `aria-current="page"` en la página activa.

## Validación estructural JSON-LD/OG (56 checks en verde)

Sobre las 5 páginas de `dist/`: JSON-LD parsea y cumple los campos mínimos (Person completo con sameAs x3 y address; SoloKey `SoftwareSourceCode` con `codeRepository`; ACP `SoftwareSourceCode` sin `codeRepository` por repo privado); canonical y hreflang es/en/x-default absolutos y cruzados por página; OG completo (`og:type` article solo en case study, locale + alternate, imagen absoluta con 1200x630 declarado Y verificado sobre el PNG real); twitter card solo con imagen; 404 sin canonical/link-alternate/twitter; sitemap con 4 URLs (sin la 404) y alternates `xhtml:link`; robots.txt con sitemap absoluto.

### Guion de validadores externos (dueño, tras el deploy de F7 o pegando HTML)

1. **Rich Results Test** (<https://search.google.com/test/rich-results>): probar `https://angelezequiel.dev/` y `/acp-suite/` (o pegar el HTML de `dist/`). Esperado: sin errores de sintaxis; Person/SoftwareSourceCode no generan tarjeta rica propia, basta "página válida".
2. **Schema validator** (<https://validator.schema.org/>): pegar el HTML de las 4 páginas; esperado 0 errores.
3. **OG previewer**: LinkedIn Post Inspector (<https://www.linkedin.com/post-inspector/>) y <https://www.opengraph.xyz/> con las 4 URLs; esperado imagen 1200x630 por idioma, título y descripción correctos.

## Contraste AA (cálculo WCAG por par de tokens)

| Par | Claro | Oscuro |
| --- | --- | --- |
| ink / paper | 16.29 | 15.49 |
| ink / surface | 17.77 | 14.20 |
| ink-soft / paper | 7.78 | 7.83 |
| ink-soft / surface | 8.49 | 7.18 |
| accent / paper (links, badges) | 5.67 | 8.33 |
| accent / surface | 6.18 | 7.64 |
| paper / accent (botón primario) | 5.67 | 8.33 |
| signal / paper | 3.65 (solo texto grande/gráficos, doc 05) | 8.52 |

Todos los pares de texto normal >= 4.5:1 (AA) en ambos temas; `--color-signal` claro sigue restringido por regla y hoy no se usa en `src/`.

## Reglas duras (grep sobre src/)

0 em dash / en dash, 0 emojis, 0 Redis / Supabase / OAuth2. "microservic" solo en la negación deliberada del case study (línea "no microservicios", encuadre que pide la regla 2; igual que ronda F2).

## Guion NVDA para el dueño (pase manual pendiente)

Con `npm run preview` corriendo y NVDA activo (Ctrl+Alt+N), en Chrome o Firefox:

1. Abrir `http://localhost:4321/`. Insert+F7: lista de encabezados con UN h1 ("Llevo sistemas completos...") y h2 por sección, sin saltos de nivel.
2. Tab desde la barra de direcciones: primer foco "Saltar al contenido"; Enter debe llevar al contenido principal.
3. Recorrer el header con Tab: logo, "Inicio" (anunciado como "página actual"), "Case study", "Contacto", botón "Cambiar a tema oscuro" (activar con Enter: el label debe pasar a "Cambiar a tema claro" y la página oscurecer), link "Switch to English" (se pronuncia en inglés).
4. En el hero: la línea de estado se lee como "en producción · v2026.07" sin anunciar el punto decorativo.
5. En certificaciones: cada link se anuncia "Ver credencial (se abre en una pestaña nueva)".
6. Ir a `/acp-suite/`: navegar por landmarks (tecla D) y encabezados (H); el nav "Contenido" (TOC) lista las secciones; al llegar al diagrama debe leerse su título y descripción larga (el flujo completo de la arquitectura).
7. Abrir `http://localhost:4321/no-existe/`: el bloque ES se lee en voz española y el bloque EN cambia de idioma/voz.
8. Verificar que nada se anuncia dos veces y que no hay elementos "clickeable" sin nombre.

## Code review del diff (master..develop + F6)

`/code-review` a esfuerzo alto sobre el diff completo del rediseño (10 ángulos: 2 corrieron como agentes, los 8 restantes se ejecutaron inline en la misma sesión al toparse el límite del plan; la cobertura por ángulo se mantuvo). `frontend-code-review` sobre `src/` con su checklist (class:list, Tailwind-first, orden de className): sin hallazgos; los estilos scoped de CaseStudyArticle/Carousel/AcpArchitecture están justificados (prosa vía set:html, estados del carrusel, SVG interno).

### Hallazgos corregidos en esta ronda (commit `fix(review): ...`)

| Hallazgo | Severidad | Corrección |
| --- | --- | --- |
| El snap del carrusel calculaba `offsetLeft` contra `body` (el track no era contenedor posicionado): al soltar el arrastre apuntaría a coordenadas de página, no de la tira. Latente (el carrusel aún no se monta, D8) | Alta (latente) | `position: relative` en `.carousel-track` |
| `Base.astro` importaba `motion.ts` incondicionalmente: la 404 y los usuarios con reduced motion descargaban ~44 KB gzip de GSAP para no animar nada | Media | Gate con `querySelector('[data-boot],[data-reveal]')` + reduced motion e import dinámico; motion queda en chunk propio |
| El script del carrusel importaba Draggable estáticamente: con reduced motion se descargaba código muerto (~13 KB gzip) | Media (latente) | `import('gsap')`/`import('gsap/Draggable')` dinámicos solo al mejorar; los controles no necesitan GSAP |
| La redirección de idioma perdía query string y hash (`/?utm=x#contacto` → `/en/`) | Baja | Se anexan `location.search + location.hash` |
| El h2 de la prosa del case study usaba `letter-spacing: 0.03em` vs los `0.025em` de `tracking-wide` del resto de eyebrows (divergencia ya ocurrida del cluster repetido) | Baja | Alineado a 0.025em con comentario |
| Claves muertas en `ui.ts` (`localeNames`, `contact.email`) | Baja | Eliminadas |

### Hallazgos diferidos (reutilización, sin riesgo funcional; no bloquean el gate)

Refactors de consolidación reportados por el review que se difieren a una ronda `chore/` posterior al lanzamiento, para no meter churn en el gate: extraer `KeyNumbers.astro` (tira de cifras duplicada entre teaser y case study), generalizar `CredentialLink` a `ExternalLink.astro` (patrón externo triplicado con Contact y Projects), utilidad `eyebrow` para el cluster mono repetido 8 veces, helper `getCaseStudy(slug, locale)` (lookup triplicado), compartir tokens/pipeline de fuentes entre `scripts/og-images.mjs` y `scripts/favicons.mjs`, y cachear la geometría de `nearestIndex()` del carrusel. Riesgo aceptado y documentado: si cambia el copy de `ui.ts` o el frontmatter del case study, hay que regenerar las imágenes OG (`scripts/og-images.mjs` espeja ese copy a mano); en F6 no cambió ninguno de esos strings, las OG siguen en sync. Los hex de theme-color en `Base.astro` espejan `--paper` en 3 puntos comentados (restricción del anti-FOUC); se acepta.

## Verificación al cerrar

- `astro check`: 0 errores / 0 warnings / 1 hint (el conocido de `z.string().url()`, F2).
- `astro build` + `preview`: 5 páginas + sitemap, todas 200; 404 real para rutas inexistentes.
- Lockfile y `package.json` intactos (playwright-core y lighthouse via `--no-save`/`npx`, mismo patrón que F5).

## Pendiente al cerrar

- **Dueño**: pase NVDA (guion arriba), validadores externos (guion arriba), D7 (beacon + CSP) y D3/D4 (merge a master y verificación del deploy).
- El punto de F6 "prueba manual de teclado + NVDA" queda cubierto en su mitad automatizable (teclado, 34 checks e2e); la mitad NVDA es del dueño por naturaleza (audio).
- Siguiente: **F7 (lanzamiento y perfil)** tras el visto bueno del dueño.
