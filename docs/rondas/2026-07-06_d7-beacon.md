# Ronda 2026-07-06 · D7: beacon de Cloudflare Web Analytics + CSP

Cierra el único punto abierto de F5: el dueño resolvió D7 (entregó el token del beacon) y se integró la analítica con su CSP, como manda el doc 06 ("la meta CSP se agrega junto con el beacon"). Rama de trabajo: `feat/beacon-analytics` desde `develop`.

## Contexto de D7

- El sitio `angelezequiel.dev` ya existía en Web Analytics (panel de Cloudflare) en modo inyección automática, que solo funciona con tráfico proxied (nube naranja). Como el dominio apunta a GitHub Pages en modo DNS only (ADR 0007), se cambió a **"Enable with JS Snippet installation"** y el dueño entregó el snippet con el token `6c62f432341546c2b7e12b9ec22453b7` (público por naturaleza: viaja en el HTML).
- El mismo día el dueño resolvió D9: apex y `www` a `ezequielangel0.github.io` en DNS only, custom domain guardado y "DNS check successful" en GitHub Pages.

## Cambios (solo `src/layouts/Base.astro`)

- **Beacon**: `<script is:inline defer src="https://static.cloudflareinsights.com/beacon.min.js" data-cf-beacon='{"token": ...}'>` al final del `<body>`. Solo en build de producción (`import.meta.env.PROD`) para no registrar visitas de desarrollo. No bloquea nada y el sitio funciona igual sin él (ADR 0006).
- **Meta CSP** al inicio del `<head>`, también solo en producción (en dev, Vite inyecta scripts propios y el websocket de HMR):

  `default-src 'self'; script-src 'self' 'unsafe-inline' https://static.cloudflareinsights.com; style-src 'self' 'unsafe-inline'; img-src 'self'; font-src 'self'; connect-src 'self' https://cloudflareinsights.com https://static.cloudflareinsights.com; base-uri 'self'; form-action 'self'`

### Decisión: `'unsafe-inline'` en vez de hashes

Inventario del HTML construido: cada página lleva 2 scripts inline clásicos (anti-FOUC de tema y detección de idioma, este último con `define:vars`, que varía por página), 1 script `type="module"` inline (Astro inlinea los hoisted pequeños) y el case study además un `<style>` inline. Hashes exigirían recalcularlos ante cualquier cambio de esos scripts, de `define:vars` o de la serialización del bundler: frágiles y con beneficio casi nulo en un sitio estático sin entrada de usuario (mismo razonamiento de la decisión F5, doc 06). El valor real de la política es el control de orígenes: nada de terceros salvo el beacon, `base-uri` y `form-action` cerrados. `connect-src` incluye `cloudflareinsights.com` porque el beacon hace POST a `/cdn-cgi/rum` en ese host (el sitio no está proxied, así que el POST no es relativo).

## Verificación

- `astro check`: 0 errores / 0 warnings / 1 hint (el conocido de `z.string().url()`, F2).
- `astro build` + `astro preview`: 5 páginas; meta CSP y beacon presentes en las 5 (token parseable desde el atributo).
- Smoke con Chrome headless (playwright-core de F6, `--no-save`; lockfile intacta) sobre `/`, `/en/`, `/acp-suite/` y `/no-existe/` (404): **0 eventos `securitypolicyviolation`** en las 4 rutas; toggle de tema funciona bajo CSP (`data-theme` cambia a `dark`); chunk de `motion` carga y el hero queda en opacidad 1; el beacon `beacon.min.js` carga desde `static.cloudflareinsights.com` sin violación.
- Hallazgo esperado, no bloqueante: el POST del beacon a `cloudflareinsights.com/cdn-cgi/rum` falla por CORS **solo en localhost con puerto** (Cloudflare responde `Access-Control-Allow-Origin: http://localhost`, sin el `:4321`). No es la CSP (0 violaciones) ni un bug del sitio: en producción el origen es `https://angelezequiel.dev` y el POST pasa. Efecto lateral útil: el preview local no ensucia las métricas.
- Reglas duras sobre el diff: sin em dash, sin emojis, sin términos vetados.

## Pendiente al cerrar

- **Dueño**: activar Enforce HTTPS cuando termine la emisión del certificado (D9, ya en curso), merge de `develop` a `master` (D3) y verificación del deploy (D4). Tras el deploy, confirmar en el panel de Web Analytics que registran visitas (el POST del beacon solo funciona en el dominio real).
- **Agente**: reescribir `README.md` del repo tras el deploy verificado (F7).
