# Ronda 2026-07-06 · Fase F2 (contenido)

Transcribe el contenido real a `src/content/` y `src/data/`, monta las colecciones tipadas y pasa las reglas duras. Rama de trabajo: `feat/contenido` desde `develop`.

## Orden de trabajo y estado

- [hecho] **Colecciones.** `src/content.config.ts` con esquemas Zod estrictos para `case-study` (title, slug, locale, tagline, keyNumbers[], stack[], screenshots? opcional D8) y `projects` (title, slug, locale, summary, stack[], repoUrl nullable, status, dates, featured, image?/imageAlt? opcionales D8). Loader `glob` con `generateId` por locale.
- [hecho] **Case study.** `case-study/acp-suite.es.md` y `.en.md` transcritos tal cual desde `perfil-mejorado/CASE_STUDY_ACP_ES_EN.md`, sin cambiar datos. tagline, keyNumbers (las 7 cifras autorizadas) y stack al frontmatter; el cuerpo omite esas dos líneas. Diagrama Mermaid reemplazado por marcador (el SVG artesanal es F4, doc 04).
- [hecho] **Proyectos.** `projects/solokey.es.md` y `.en.md` con los datos de doc 02 (zero-trust, offline-first, Argon2id + AES-256-GCM vía Android KeyStore, TOTP, auditoría de débiles), `status: public-repo`, repo público, fechas feb-mar 2026 (de `CV_contenido_EN.md`). ACP no entra aquí (es el case study).
- [hecho] **Datos.** Nuevos `src/data/certifications.json` (las 3 con `credentialUrl`) y `skills.json` (5 grupos de doc 02, nombres ES/EN, items bilingües neutrales). Borrados los 4 JSON viejos huérfanos (`proyectos.json`, `certificados.json`, `emisores.json`, `tags.json`).
- [hecho] **i18n.** `src/i18n/ui.ts` ampliado con hero (línea de estado, titular D2, subtítulo, CTAs), secciones, badges de estado, "ver credencial", eyebrow y labels del case study, y lead de contacto. Paridad ES/EN forzada por el tipo `UiKey`.
- [hecho] **Validación de pares.** `src/lib/content.ts` (`assertLocalePairs` + `getLocalized`): falla el build si un slug no existe en ambos idiomas. Se cablea en los `getStaticPaths` de F3/F4.
- [hecho] **CV.** `perfil-mejorado/Angel_Barbosa_Resume.pdf` copiado a `public/cv/Angel_Barbosa_CV.pdf` (29505 bytes, PDF válido); borrado el viejo `public/Angel_Ezequiel_Barbosa_Resume.pdf`.

## Hallazgos y decisiones de la ronda

1. **JSON viejos con datos prohibidos.** F0 no borró `src/data/*.json` del sitio anterior. `proyectos.json` contenía "Supabase" y datos falsos de ACP (Spring, repo público); `tags.json` usaba colores inexistentes. Nadie los importaba (`index.astro` solo los nombra en prosa). Borrados: sin ellos el grep de reglas duras queda limpio.
2. **`glob` colapsaba los pares ES/EN.** Por defecto el loader daba el mismo id ("acp-suite", "solokey") a los dos idiomas y uno sobrescribía al otro (warning explícito de Astro). Corregido con `generateId: ({ entry }) => entry.replace(/\.md$/, '')` (id `slug.locale`). Verificado: las 4 entradas presentes en el data store.
3. **Diagrama del case study.** El bloque Mermaid del fuente no se renderiza en F2 (sin dependencia Mermaid en cliente ni build, doc 03/04). Queda un marcador HTML en la sección Arquitectura; el SVG artesanal tematizado por idioma se hace en F4.
4. **tagline/keyNumbers/stack al frontmatter.** El diseño (doc 05) los pinta como panel de cifras y tira de stack, no como prosa; por eso salen del cuerpo al frontmatter tipado. El cuerpo conserva problema, solución, arquitectura, seguridad, operación, calidad y lecciones, verbatim.
5. **Copy del home en `ui.ts`.** La arquitectura no define colección de "home"; el copy corto del hero y las secciones vive en el diccionario tipado (lo consume F3). El contenido largo sigue en `src/content/`.
6. **`z` de `astro:content` deprecado en Astro 7.** Cambiado a `import { z } from 'astro/zod'` (limpia 24 hints; queda 1 hint no relacionado).

## Verificación

- `astro check`: 0 errores / 0 warnings / 1 hint.
- `astro build`: 2 páginas + sitemap; contenido sincronizado sin warnings de id duplicado; 4 entradas (`acp-suite.es/en`, `solokey.es/en`) en el data store.
- `astro preview`: `/`, `/en/` y `/cv/Angel_Barbosa_CV.pdf` responden HTTP 200.
- **Reglas duras (grep sobre `src/`):** 0 de `Redis`, `Supabase`, `OAuth2`, em dash, en dash y emojis. `microservic` da 2 hits, ambos la negación deliberada "no microservicios" / "not microservices" del case study: es el encuadre que exige la regla 2 (doc 02), no una violación. Se conserva por precedencia de la fuente (doc 04) y por ser técnicamente correcto.

## Pendiente al cerrar

- Las páginas aún no renderizan el contenido (es F3/F4); F2 solo deja el modelo, los datos y el diccionario listos y validados.
- La invariante de pares ES/EN se activa como gate de build al cablear `getLocalized`/`assertLocalePairs` en los `getStaticPaths` de F3/F4.
- Capturas opcionales (D8): los esquemas ya tienen `screenshots?`/`image?`; el carrusel llega en F4.
- Siguiente: **F3 (home)**. Ver PENDIENTES_AGENTE y doc 07.
